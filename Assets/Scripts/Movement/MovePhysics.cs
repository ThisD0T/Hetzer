using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Scamper.Movement {
    public class MovePhysics : MonoBehaviour
    {
        public Vector3 Accelerate(Vector3 currentVelocity, Vector3 wishdir, float wishspeed) {
            return currentVelocity;
        }

        public Vector3 AirAccelerate(Vector3 currentVelocity, Vector3 wishdir, float wishspeed) {
            return currentVelocity;
        }
    }
}
